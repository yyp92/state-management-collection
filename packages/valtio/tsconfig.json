{
    // 继承根目录配置文件
    "extends": "../../tsconfig.json",

    // 编译选项
    "compilerOptions": {
        // 设置应用别名
        "paths": {
            "valtio": ["./src/index.ts"],
            "valtio/*": ["./src/*.ts"],
        },
    },

    // 限定类型检查范围
    "include": ["src"],
}
